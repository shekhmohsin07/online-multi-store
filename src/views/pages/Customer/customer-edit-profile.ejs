<link rel="stylesheet" href="/css/store/store-registration.css">

<div class="form-container">
  <h2>Update User Profile</h2>
  <% if(error.length>0){ %>
      <div class="errorContainer">
        <% error.forEach(element => { %>
          <p><%= element.msg %></p>
        <% }); %>
        <div class="crossError"><i class="fa-solid fa-xmark"></i></div>
      </div>
    <% } %>
  <form id="userForm" action="/edit-profile/<%= user._id %>?_method=PUT" method="post" enctype="multipart/form-data">
    <div>
      <label for="customerusername">User Name <span class="required">*</span></label>
      <input type="text" id="customerusername" value="<%= user.customerusername %>" name="customerusername" required>
    </div>

     <div>
      <label for="customerprofilephoto">Profile Photo <span class="required">*</span></label>
      <input type="file" id="customerprofilephoto" name="customerprofilephoto" accept="image/*" required>
    </div>

    <div>
      <label for="customeremail">Email Address <span class="required">*</span></label>
      <input type="email" id="customeremail" value="<%= user.customeremail %>" name="customeremail" required>
    </div>
    <div>
      <label for="whatsapp">WhatsApp Number <span class="required">*</span></label>
      <div class="phonenumber">
        <select id="selectcountryisd" name="countryisd">
          <option value="+974" selected>+974</option>
        </select>
        <input type="number" id="whatsapp" name="customerwhatsapp" value="<%= user.customerwhatsappnumber %>" placeholder="501234567" required>
      </div>
    </div>

    <div>
      <label for="customeraddressunitno"> Unit No. <span class="required">*</span></label>
      <input type="number" id="customeraddressunitno" value="<%= user.customerunitno %>" name="customerunitno" required>
    </div>
    <div>
      <label for="customeraddressbuildingno"> Building No. <span class="required">*</span></label>
      <input type="number" id="customeraddressbuildingno" value="<%= user.customerbuildingno %>" name="customerbuildingno" required>
    </div>
    <div>
      <label for="customeraddressstreetno"> Street No. <span class="required">*</span></label>
      <input type="number" id="customeraddressstreetno" value="<%= user.customerstreetno %>" name="customerstreetno" required>
    </div>
    <div>
      <label for="customeraddresszoneno"> Zone No. <span class="required">*</span></label>
      <input type="number" id="customeraddresszoneno" value="<%= user.customerzoneno %>" name="customerzoneno" required>
    </div>
    <div>
      <label for="customercountry">Country <span class="required">*</span></label>
      <select id="customercountry" name="customercountry" required>
        <option value="Qatar" selected>Qatar</option>
      </select>
    </div>
    <div class="dropdown-subcategory">
      <label for="customercity">Area <span class="required">*</span></label>
      <input id="customercity" type="text" value="<%= user.customercity %>" name="customercity" required>
      <div class="dropdown-list" id="dropdown"></div>
    </div>
    <div>
      <label for="customerlocation">Customer Map Location <span class="required">*</span></label>
      <span class="showlocationdisplay"><%= user.customerlocation %></span>
      <input type="hidden" id="customerlocation" value="<%= user.customerlocation %>" name="customerlocation" required>
      <div class="picklocation">
          <span >Select  location from Map</span>
      </div>
    
    </div>
    

    <button class="submitBTN" type="submit">Update Profile</button>
  </form>
  <div class="mapcontainer">
            <h3>Pick a Location</h3>
            <input
            id="searchBox"
            type="text"
            placeholder="Search location..."
            />
            <div id="map" ></div>
            <div class="confiremmaplocation">Confirm Selection</div>
  </div>
</div>


<script src="https://maps.googleapis.com/maps/api/js?key=<%= process.env.GOOGLE_MAPS_API_KEY %>&libraries=places"></script>
<script src="/js/customerjs/customer-registration.js"> </script>