<link rel="stylesheet" href="/css/store/seller-login.css">
<div class="loginformcontainer">
    <div class="heading_login"><h1>Customer Login</h1></div>
    <% if(error.length>0){ %>
      <div class="errorContainer">
        <% error.forEach(element => { %>
          <p><%= element.msg %></p>
        <% }); %>
        <div class="crossError"><i class="fa-solid fa-xmark"></i></div>
      </div>
    <% } %>
    <% if (success_msg!='') { %>
    <div style="background-color: green !important;" class="errorContainer">
        <p style="color: #fff !important;"><%= success_msg %></p>
      <div class="crossError"><i class="fa-solid fa-xmark"></i></div>
    </div>
  <% } %>
    <form id="sellerloginform" action="/login" method="post">
        <div class="input_email">
            <label for="loginuseremail">Email Address</label>
            <input type="email" name="loginuseremail" id="loginuseremail" placeholder="user@mail.com" required>
        </div>
        <div class="input_pass">
            <label for="loginuserpass">Password</label>
            <input type="password" name="loginuserpass" id="loginuserpass" required>
            <i class="fa-solid fa-eye eyespass"></i> 
        </div>
        <div class="forgetpass">
           <p>Don't have Account? <a href="/registration">Create Profile</a>.</p> 
           <p>Forget Password? <a href="/forgetpassword">Click here</a></p>
        </div>
        <button type="submit">Login</button>
    </form>
</div>

<script src="/js/storejs/store-login.js"></script>