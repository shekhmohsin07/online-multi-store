<link rel="stylesheet" href="/css/store/store-registration.css">
<% if(error.length>0){ %>
    <div class="errorContainer">
      <% error.forEach(element => { %>
        <p><%= element.msg %></p>
      <% }); %>
      <div class="crossError"><i class="fa-solid fa-xmark"></i></div>
    </div>
<% } %>
<% if (error_msg!='') { %>
    <div style="background-color: red !important;" class="errorContainer">
        <p><%= error_msg %></p>
      <div class="crossError"><i class="fa-solid fa-xmark"></i></div>
    </div>
  <% } %>

<div class="form-container">
  <h2> Store Registration Form</h2>
  <form id="storeForm" action="/registration-store" method="post" enctype="multipart/form-data">
    <div>
      <label for="storeName">Store Name <span class="required">*</span></label>
      <input type="text" id="storeName" <% if(oldData){ %> value="<%= oldData.storeName %>" <% } %> name="storeName" required>
    </div>
    <div>
      <label for="ownerName">Owner Name <span class="required">*</span></label>
      <input type="text" id="ownerName" <% if(oldData){ %> value="<%= oldData.ownerName %>" <% } %> name="ownerName" required>
    </div>

     <div>
      <label for="storePhoto">Store Photo <span class="required">*</span></label>
      <input type="file" id="storePhoto" name="storePhoto" accept="image/*" required>
    </div>

    <div>
      <label for="storeemail">Email Address <span class="required">*</span></label>
      <input type="email" id="storeemail" <% if(oldData){ %> value="<%= oldData.storeemail %>" <% } %> name="storeemail" required>
    </div>
    <input type="hidden" id="countryisocode" <% if(oldData){ %> value="<%= oldData.countryisocode %>" <% } %> name="countryisocode">
    <div>
      <label for="storeCategory">Store Category <span class="required">*</span></label>
      <select id="storeCategory" name="storeCategory" required>
        <option value="grocery" selected>Grocery</option>
        <option value="restaurant">Restaurant</option>
      </select>
    </div>

    <div>
      <label for="whatsapp">WhatsApp Number <span class="required">*</span></label>
      <div class="phonenumber">
        <select id="selectcountryisd" name="countryisd">
          <option value="+974" selected>+974</option>
        </select>
        <input type="number" id="whatsapp" name="whatsapp" <% if(oldData){ %> value="<%= oldData.whatsapp %>" <% } %> placeholder="501234567" required>
      </div>
    </div>

    <div>
      <label for="storeunitno"> Unit No. <span class="required">*</span></label>
      <input type="number" id="storeunitno" <% if(oldData){ %> value="<%= oldData.storeunitno %>" <% } %> name="storeunitno" required>
    </div>
    <div>
      <label for="storebuildingno"> Building No. <span class="required">*</span></label>
      <input type="number" id="storebuildingno" <% if(oldData){ %> value="<%= oldData.storebuildingno %>" <% } %> name="storebuildingno" required>
    </div>
    <div>
      <label for="storestreetno"> Street No. <span class="required">*</span></label>
      <input type="number" id="storestreetno" <% if(oldData){ %> value="<%= oldData.storestreetno %>" <% } %> name="storestreetno" required>
    </div>
    <div>
      <label for="storezoneno"> Zone No. <span class="required">*</span></label>
      <input type="number" id="storezoneno" <% if(oldData){ %> value="<%= oldData.storezoneno %>" <% } %> name="storezoneno" required>
    </div>

    

    <div>
      <label for="country">Country <span class="required">*</span></label>
      <select id="country" name="country" required>
        <option value="Qatar" selected>Qatar</option>
      </select>
    </div>
    <div class="dropdown-subcategory">
      <label for="storecity">Area <span class="required">*</span></label>
      <input id="storecity" type="text" <% if(oldData){ %> value="<%= oldData.storecity %>" <% } %> name="storecity" required>
      <div class="dropdown-list" id="dropdown"></div>
    </div>
    <div>
      <label for="storelocationmap">Store Google Map Location <span class="required">*</span></label>
      <span class="showlocationdisplay"></span>
      <input type="hidden" id="storelocationmap" name="storelocationmap" <% if(oldData){ %> value="<%= oldData.storelocationmap %>" <% } %> required>
      <input type="hidden" id="storelocationmaplat" name="storelocationmaplat" <% if(oldData){ %> value="<%= oldData.storelocationmaplat %>" <% } %> required>
      <input type="hidden" id="storelocationmaplng" name="storelocationmaplng" <% if(oldData){ %> value="<%= oldData.storelocationmaplng %>" <% } %> required>
      <div class="picklocation">
          <span >Select  location from Map</span>
      </div>
    
    </div>

    <div>
      <label for="storeopen">Store Working Hours from <span class="required">*</span></label>
      <input type="time" id="storeopen" name="storeopen" <% if(oldData){ %> value="<%= oldData.storeopen %>" <% } %> placeholder="e.g. 9 AM - 10 PM" required>
    </div>
    <div>
      <label for="storeclose">Store Working Hours Till <span class="required">*</span></label>
      <input type="time" id="storeclose" name="storeclose" <% if(oldData){ %> value="<%= oldData.storeclose %>" <% } %> placeholder="e.g. 9 AM - 10 PM" required>
    </div>
    <div>
      <label for="homeDelivery">Home Delivery <span class="required">*</span></label>
      <select id="homeDelivery" name="homeDelivery" required>
        <option value="Available" selected>Available</option>
        <option value="Not Available">Not Available</option>
      </select>
    </div>
    <div>
      <div class="passContainer">
      <label for="storepassword">Login Password <span class="required">*</span></label>
      <input type="password" id="storepassword" name="storepassword" required>
      <i class="fa-solid fa-eye eyespass"></i> 
    </div>
    <p class="passCondition">Password must contain mininum 8 chars, at least one uppercase, at least one special character and one Number</p>
    </div>
    
    <div class="checkbox-container">
      <input type="checkbox" id="terms" required>
      <label for="terms" style="font-weight:normal;">
        By Clicking It You are Agree to Our All Terms and Condition
      </label>
    </div>

    <button class="submitBTN" type="submit">Register</button>
  </form>

  <div class="mapcontainer">
            <h3>Pick a Location</h3>
            <input
            id="searchBox"
            type="text"
            placeholder="Search location..."
            />
            <div id="map" ></div>
            <div class="confiremmaplocation">Confirm Selection</div>
  </div>
</div>

<script src="https://maps.googleapis.com/maps/api/js?key=<%= process.env.GOOGLE_MAPS_API_KEY %>&libraries=places"></script>
 <!-- Load Maps JS API (browser key, restricted by domain) -->
<script src="/js/storejs/store-registration.js"> </script>