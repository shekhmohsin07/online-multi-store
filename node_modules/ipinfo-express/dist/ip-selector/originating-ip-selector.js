"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var defaultIPSelector = function (req) {
    var xForwardedFor = req.header("x-forwarded-for");
    if (!xForwardedFor || xForwardedFor.trim() === "") {
        return req.ip;
    }
    else {
        var ips = xForwardedFor.split(",");
        return ips[0].trim();
    }
};
exports.default = defaultIPSelector;
